
由于QE需要读取xml文件，所以使用了三方函数库 fox

添加了 QE和abinit的接口（abinit的K点设置后续要补充相关程序（K_SHIFT）），其中由于abinit的波函数文件名为用户自定义，所以在INKRPOJ添加了波函数文件名的参数（仅ABINIT使用WFC_AB）

在mod_wave中通过判断相关文件的存在情况，选择VASP-QE-ABINIT,其中QE的输出文件名为固定，所以通过固定文件名称判定，ABINIT通过判断INKPROJ中是否给出了ABINIT的波函数文件判定

step_function的计算单独写成一个模块mod_step_function.f ，为了解决VASP-QE-ABINIT循环调用的问题

添加了 ABACUS phonopy 的接口，并增加了相关输入参数的设置（见other_unfold内的example和readme），通过INKPROJ中的开关来判定进行何种计算,输出格式保持与VASP-QE-ABINIT一致(部分参数未更新，暂不可参考)

添加了 ABCUSE 计算spin_texture的功能

修改了supercell的输出，现在在pos_map中会输出原胞、超胞晶格矢量,修改了平移矢量的输出逻辑

更换了 GPFA-FFT 为 FFTW ，增加了单核多线程设置：nthread（默认值为2，因为通常单核的最大线程数为2），提升运行速度3-4倍，后续跟进MPI版本（KRPOJ-FFTW-MPI接口已经写好，也有可能使用FFTW-OPENMP进行多核并行）
其中使用的FFTW为英特尔MKL数学库内置，不需额外下载。

添加了 KPROJ的绘图小程序 kproj_plot.py（utils内） ,直接对bs_projected.dat 进行绘图，不需运行futils

编译方法 
第1步：make fox （安装QE的xml文件读取依赖） 其中make fox 通过脚本fox.sh 完成，后续会优化Makefile的书写
第2步：make


修改pos_map
